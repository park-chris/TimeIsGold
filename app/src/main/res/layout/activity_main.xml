<?xml version="1.0" encoding="utf-8"?>
<layout>

<LinearLayout
    android:id="@+id/fragment_entire_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="9" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/main_navi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/navi_menu"
        android:background="@color/white_pink"
        app:itemBackground="@color/white_pink"
        android:layout_weight="1"
        app:labelVisibilityMode="unlabeled"
        app:itemIconTint="@drawable/navi_menu_selector"
        app:itemTextColor="@drawable/navi_menu_selector"/>


    <Button
        android:id="@+id/testButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="dark theme로" />

</LinearLayout>

</layout>